# 🎣 Настройка и запуск проекта

## Шаг 1: Настройка Supabase

### 1.1. Создание проекта
1. Перейдите на [https://supabase.com](https://supabase.com)
2. Войдите или зарегистрируйтесь
3. Нажмите "New Project"
4. Заполните данные:
   - Project Name: `fishing-shop` (или любое другое)
   - Database Password: придумайте надежный пароль
   - Region: выберите ближайший регион
5. Нажмите "Create new project" и дождитесь создания (~2 минуты)

### 1.2. Выполнение SQL схемы
1. В левом меню выберите "SQL Editor"
2. Нажмите "New query"
3. Скопируйте весь SQL код из файла `supabase-schema.sql`
4. Вставьте в редактор
5. Нажмите "Run" или Ctrl+Enter
6. Убедитесь, что выполнение прошло успешно (Success)

### 1.2.1. Миграция для множественных изображений (если обновляете существующий проект)
Если у вас уже есть товары с одним изображением, выполните миграцию:
1. В "SQL Editor" создайте новый запрос
2. Скопируйте код из файла `supabase-migration-images.sql`
3. Выполните запрос
4. Все существующие изображения будут автоматически перенесены в массив

### 1.3. Настройка Storage для изображений
1. В левом меню выберите "Storage"
2. Нажмите "Create a new bucket"
3. Введите название: `product-images`
4. **Важно:** Включите опцию "Public bucket" (чтобы изображения были доступны)
5. Нажмите "Create bucket"
6. Перейдите в "SQL Editor" и выполните код из файла `supabase-storage-setup.sql`

### 1.4. Получение ключей API
1. В левом меню выберите "Settings" (шестеренка внизу)
2. Выберите "API"
3. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ (длинная строка, начинается с `eyJ...`)

## Шаг 2: Настройка локального проекта

### 2.1. Создание .env файла
В корне проекта создайте файл `.env`:

\`\`\`env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...ваш_длинный_ключ
\`\`\`

### 2.2. Установка зависимостей
\`\`\`bash
npm install
\`\`\`

### 2.3. Запуск приложения
\`\`\`bash
npm run dev
\`\`\`

Откройте браузер: [http://localhost:5173](http://localhost:5173)

## Шаг 3: Проверка работы

### 3.1. Публичный каталог
- Откройте главную страницу
- Должны отобразиться тестовые товары
- Попробуйте фильтровать по категориям
- Попробуйте прокрутить вниз (infinite scroll)
- Переключите тему (кнопка внизу справа)

### 3.2. Админ-панель
1. Перейдите на `/login` или `http://localhost:5173/login`
2. Введите логин и пароль, которые вы указали в файле `.env`:
   - **Логин**: значение `VITE_ADMIN_LOGIN` из `.env`
   - **Пароль**: значение `VITE_ADMIN_PASSWORD` из `.env`
3. Нажмите "Войти"
4. Попробуйте добавить новый товар
5. Попробуйте отредактировать существующий товар
6. Попробуйте удалить товар

## Шаг 4: Деплой на GitHub Pages

### 4.1. Настройка GitHub Secrets
1. Перейдите в ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Нажмите "New repository secret"
4. Добавьте два секрета:
   
   **Секрет 1:**
   - Name: `VITE_SUPABASE_URL`
   - Secret: ваш URL из Supabase
   
   **Секрет 2:**
   - Name: `VITE_SUPABASE_ANON_KEY`
   - Secret: ваш anon ключ из Supabase

### 4.2. Включение GitHub Pages
1. Settings → Pages
2. Source: выберите "GitHub Actions"
3. Сохраните

### 4.3. Деплой
\`\`\`bash
git add .
git commit -m "Setup project"
git push origin main
\`\`\`

GitHub Actions автоматически:
- Соберет проект
- Задеплоит на GitHub Pages

Сайт будет доступен через несколько минут по адресу:
`https://ваш-username.github.io/Everything-For-Fishing/`

## Возможные проблемы

### Проблема: "Failed to fetch"
**Решение**: Проверьте правильность URL и ключа Supabase в `.env`

### Проблема: Товары не загружаются
**Решение**: 
1. Убедитесь, что SQL схема выполнена успешно
2. Проверьте таблицы в Supabase: Table Editor → products / categories
3. Проверьте RLS политики (должны быть включены)

### Проблема: GitHub Pages показывает 404
**Решение**:
1. Убедитесь, что `vite.config.ts` содержит правильный `base`
2. Проверьте, что GitHub Actions workflow выполнился успешно
3. Подождите 5-10 минут после первого деплоя

### Проблема: После деплоя роутинг не работает
**Решение**: GitHub Pages не поддерживает client-side routing из коробки.
Решения:
1. Использовать Hash Router вместо Browser Router
2. Или использовать другой хостинг (Vercel, Netlify) - они поддерживают SPA

## Данные для входа в админ-панель

Логин и пароль администратора хранятся в файле `.env`:
- `VITE_ADMIN_LOGIN` - логин администратора
- `VITE_ADMIN_PASSWORD` - пароль администратора

⚠️ **Важно**: 
- Файл `.env` не попадает в репозиторий (указан в `.gitignore`)
- При деплое на GitHub Pages добавьте эти значения как GitHub Secrets (см. [DEPLOYMENT.md](DEPLOYMENT.md))
- Для других разработчиков используйте шаблон из `.env.example`
