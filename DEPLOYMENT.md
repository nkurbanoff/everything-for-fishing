# Инструкция по безопасному деплою на GitHub Pages

## 🔐 Безопасность

**ВАЖНО:** Файл `.env` содержит ваши реальные логин и пароль администратора, поэтому он уже добавлен в `.gitignore` и НЕ будет попадать в репозиторий.

## 📁 Что можно безопасно отправлять в публичный репозиторий

### ✅ БЕЗОПАСНО (можно пушить):
- `.env.example` - содержит только шаблон без реальных данных
- `src/` - весь исходный код
- `public/` - публичные файлы
- `.github/workflows/deploy.yml` - настройки деплоя
- `package.json`, `tsconfig.json`, `vite.config.ts` и другие конфиги
- `README.md`, `SETUP.md`, `DEPLOYMENT.md`

### ❌ НЕЛЬЗЯ пушить:
- `.env` - содержит реальные credentials (УЖЕ в .gitignore)
- `node_modules/` - зависимости (УЖЕ в .gitignore)
- `dist/` - билд (УЖЕ в .gitignore)

## 🚀 Шаги для деплоя

### 1. Создайте репозиторий на GitHub

Перейдите на [github.com/new](https://github.com/new) и создайте репозиторий с названием `Everything-For-Fishing`.

**ВАЖНО:** Репозиторий должен быть **публичным** для использования GitHub Pages бесплатно.

### 2. Настройте GitHub Secrets

Перейдите в настройки репозитория: `Settings` → `Secrets and variables` → `Actions` → `New repository secret`

Добавьте 4 секрета (значения возьмите из вашего файла `.env`):

| Name | Value |
|------|-------|
| `VITE_SUPABASE_URL` | Значение из `.env` (ваш Supabase URL) |
| `VITE_SUPABASE_ANON_KEY` | Значение из `.env` (ваш anon key) |
| `VITE_ADMIN_LOGIN` | Значение из `.env` (ваш логин администратора) |
| `VITE_ADMIN_PASSWORD` | Значение из `.env` (ваш пароль администратора) |

### 3. Включите GitHub Pages

1. Перейдите в `Settings` → `Pages`
2. В разделе "Source" выберите `GitHub Actions`
3. Сохраните изменения

### 4. Отправьте код в репозиторий

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/Everything-For-Fishing.git

# Добавьте все файлы (. env автоматически исключится благодаря .gitignore)
git add .

# Сделайте коммит
git commit -m "Initial commit: Fishing shop catalog"

# Отправьте в репозиторий
git push -u origin main
```

### 5. Дождитесь деплоя

После пуша GitHub Actions автоматически:
1. Установит зависимости
2. Соберет проект с вашими секретами
3. Задеплоит на GitHub Pages

Процесс займет 2-3 минуты. Отслеживайте прогресс во вкладке `Actions`.

### 6. Откройте сайт

После успешного деплоя сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/Everything-For-Fishing/
```

## 🔍 Проверка безопасности

Чтобы убедиться, что ничего секретного не попало в репозиторий:

```bash
# Проверьте, что .env игнорируется
git status

# Файл .env НЕ должен появляться в списке для коммита
# Если появился - удалите его из git:
git rm --cached .env
```

## 📝 Обновление сайта

Чтобы обновить сайт после изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически пересоберет и задеплоит сайт.

## 🆘 Если что-то пошло не так

### Проблема: Сайт не открывается
**Решение:** Проверьте, что в `Settings` → `Pages` выбран источник "GitHub Actions"

### Проблема: Ошибка при деплое
**Решение:** Проверьте логи во вкладке `Actions` и убедитесь, что все 4 секрета добавлены правильно

### Проблема: Логин не работает
**Решение:** Проверьте, что секреты `VITE_ADMIN_LOGIN` и `VITE_ADMIN_PASSWORD` совпадают с вашими данными

## 🎉 Готово!

Теперь ваш магазин доступен онлайн, а ваши credentials в безопасности!
